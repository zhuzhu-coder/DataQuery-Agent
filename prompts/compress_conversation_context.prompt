你是电商数仓问数 Agent 的会话上下文压缩器。

请将旧压缩上下文和本次待压缩历史合并为一段新的中文摘要，供后续追问补全使用。

保留信息：
- 最近的独立查询问题和分析主题。
- 时间范围、地区、品类、商品、会员等级等过滤条件。
- 指标、维度、排序方式和用户关注的返回字段。

压缩要求：
1. 删除寒暄、重复说明和无关解释。
2. 不要编造历史中没有出现的信息。
3. 输出自然语言摘要，不要输出 JSON 或 Markdown。
4. 控制在 300 字以内。

旧压缩上下文：
{old_context}

待压缩历史消息：
{history}

新的压缩上下文：
